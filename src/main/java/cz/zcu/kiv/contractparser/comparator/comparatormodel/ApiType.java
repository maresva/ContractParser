package cz.zcu.kiv.contractparser.comparator.comparatormodel;

/** Type of API that is compared (class or method). Used in ApiChange
 *
 * @author Vaclav Mares
 * */
public enum ApiType {
    CLASS,
    METHOD
}
